#include <stdio.h>

/**
 * main - prints the phrase "Programming is like building a multiligual puzzle
 * Return: 0 if existed properly, non-zero otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multiligual puzzle");
	return (0);
}
